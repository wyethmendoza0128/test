# Provider schedule interface prototype

Static prototype that recreates the multi-room day schedule UI from the screenshot.

## Run locally

From the repo root:

```bash
python3 -m http.server 4173
```

Then open:

- `http://localhost:4173/index.html`
